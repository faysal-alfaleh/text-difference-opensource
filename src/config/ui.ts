export const uiConfig = {
  mobileBreakpointPx: 768,
  reducedMotionQuery: "(prefers-reduced-motion: reduce)",
  sidebar: {
    width: "16rem",
    widthMobile: "18rem",
    widthIcon: "3rem",
    keyboardShortcut: "b",
  },
  confetti: {
    count: 12,
    distancePx: 64,
  },
} as const
